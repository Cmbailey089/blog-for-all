<div class="container">
  <h2>{{comment.title}}</h2>
  
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
    <p class="container">{{comment}}</p>
  <p>
    <button class="btn btn-sm btn-danger" data-id="{{comment.id}}">DELETE</button>
  </p>
</div>